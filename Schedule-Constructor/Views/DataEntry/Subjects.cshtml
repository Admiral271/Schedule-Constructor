@model Schedule_Constructor.Models.DataModels.Subject

<div class="card">
    <div class="card-header">
        Добавление нового предмета
    </div>
    <div class="card-body">
        <form asp-action="CreateSubject" asp-controller="DataEntry" method="post">
            <div class="form-group">
                <label asp-for="Name_Subject">Название предмета</label>
                <input asp-for="Name_Subject" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Cabinet">Кабинет</label>
                <input asp-for="Cabinet" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Teacher">Преподаватель</label>
                <input asp-for="Teacher" class="form-control" />
            </div>
            <br />
            <button type="submit" class="btn btn-danger">Добавить</button>
        </form>
    </div>
</div>

@section Scripts {
    <script type="text/javascript">
        $(function () {
            var messageType = '@TempData["messageType"]';
            var message = '@TempData["message"]';
            if (message) {
                if (messageType === 'success') {
                    toastr.success(message);
                } else if (messageType === 'error') {
                    toastr.error(message);
                }
            }
        });
    </script>
}
